import React from 'react';
import SearchForm from './SearchForm';
import TextField from "@mui/material/TextField";
import { LoadingButton } from '@mui/lab';
import { Grid } from '@mui/material';

describe('SearchForm', () => {

  it()
 
  // it('should render a <Grid />', () => {
  //   expect(searchForm.find(Grid).length).toEqual(1);
  // })
 

  // it('should render a <TextField />', () => {
  //   expect(searchForm.find(TextField).length).toEqual(1);
  // })


  // it('should render a <LoadingButton />', () => {
  //   expect(searchForm.find(LoadingButton).length).toEqual(1);
  // })

  // it('should call the handleInputChange on input change', () => {
  //   const searchFormInst = mount(<SearchForm  />)
  //   const spy = jest.spyOn(searchFormInst.instance(), 'handleInputChange');
  //   //expect(spy).toHaveBeenCalledTimes(0);

  //   let title = searchForm.find(TextField);
  //   title.simulate('change', {
  //     target: {
  //       value: "matrix"
  //     }
  //   })
  //   expect(spy).toBeCalled();

    

// email.simulate('change', {
//     target: {
//         value: 'miqueias@gmail.com'
//     }
// });

// email = wrapper.find('#email');
// expect(email.prop('value')).toEqual('miqueias@gmail.com');

//  })

  // it('should call the handleInputChange fnc when input is updated to update state', () => {
  //   //const textField = shallow
  //   // const instance = searchForm.instance();
  //   // const value = "matrix";
  //   // instance.handleInputChange({
  //   //   title: value,
  //   // })
  // })
  


})